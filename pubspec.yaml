name: hesett_print_bridge
description: Local print bridge for Hesett (WebSocket -> raw TCP/USB/OS printing ESC/POS)
version: 0.1.0
publish_to: "none"

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  ffi: ^2.1.0



